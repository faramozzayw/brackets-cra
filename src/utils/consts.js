export const left = "left";
export const right = "right";

export const space = 32;

export const state = {
	resolve: "resolve",
	reject: "reject",
	pending: "pending",
};

export const bracketsCodePoint = {
	left: 40,
	right: 41,
};
